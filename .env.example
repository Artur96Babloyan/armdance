# Optional: public base URL for images & videos (Cloudflare R2, AWS S3 + CloudFront,
# Supabase Storage public URL, Vercel Blob custom domain, etc.).
# Mirror the same file names/paths as in /public (e.g. logo.jpg at {BASE}/logo.jpg).
# Example: https://pub-xxxxx.r2.dev
# Example: https://xxxx.supabase.co/storage/v1/object/public/your-bucket
NEXT_PUBLIC_MEDIA_BASE_URL=https://pub-xxxxxxxxxxxx.r2.dev
